// Ambient declarations for untyped dependencies.

// `@digitalbazaar/zcap-context` ships no type declarations; it exports the
// `CONTEXT` (the zcap JSON-LD context document) and `CONTEXT_URL` constants.
declare module '@digitalbazaar/zcap-context'
